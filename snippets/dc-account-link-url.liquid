{%- comment -%}
  Account link target for header / nav. Uses login URL when logged out.
{%- endcomment -%}
{%- if customer -%}{{ routes.account_url }}{%- else -%}{{ routes.account_login_url }}{%- endif -%}
